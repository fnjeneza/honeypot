# Honeypot's Config file

database:
    type: sqlite
    uri: 127.0.0.1
    user: test
    password: testpw

ldap:
    uri: 127.0.0.1
    port: 386
    base: dc=honeypot,dc=com
    user: cn=admin,dc=honeypot,dc=com
    password: admin_ldap
    base_object: ou=fake user,dc=honeypot,dc=com

log: /var/log/honeypot/ #where logs are saved


